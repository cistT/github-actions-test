name: Caller Job
on:
  workflow_dispatch:

jobs:
  steps:
    name: 同時実行
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        node:
          - test1
          - test2
          - test3
          - test4
          - test5
          - test6
          - test7
    uses: ./.github/workflows/callee.yml
    with:
      name: ${{ matrix.node }}

    # runs-on:
    #   with: sanro
    # with:
    #   name: sanro
